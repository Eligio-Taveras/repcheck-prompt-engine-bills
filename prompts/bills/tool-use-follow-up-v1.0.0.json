{
  "name": "tool-use-follow-up",
  "stage": "custom",
  "weight": 1.0,
  "version": "v1.0.0",
  "content": "PLACEHOLDER WORDING — author the production prompt content before promoting to stg/prod.\nYou have a set of tools. Use them only when the information you have is insufficient to answer confidently. Prefer a single decisive answer: gather what you need, reason once, and call `submit` when confident. Iterate with tools only if a specific gap remains. Never call a tool you were not granted. When you are ready, end by calling `submit` with output that matches the required schema exactly."
}
